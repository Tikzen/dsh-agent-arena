# 更新说明

## 0.3.0

这是 Agent Arena 从“多模型同时回复”走向“长期多 AI 协作空间”的一次完整升级。

### 持续讨论与完整 Agent 能力

- 会议改为类似群聊的持续对话，不再受固定轮次限制；人类可以随时发言、`@AI`、静默成员或立即停止当前工作。
- 每位 AI 用户使用独立的完整 DSH Agent 会话，继承当前 Agent Preset 的工具、技能和子 Agent 能力。
- 同批角色可以并发工作；角色能够按照真实进度自然发送一条或多条消息，不再机械切分长回复，也不强制每次先发送确认话术。
- 每次新消息后，角色都会结合完整聊天记录、自己的历史发言和人格判断是否继续接话；话题跑偏或自然收束时会停止自动讨论。

### AI 用户、私聊与群聊

- 新增可复用的 AI 用户库，可配置昵称、头像、人格、预设对话、供应商和模型；人格允许留空。
- 新增人类用户资料、AI 私聊和 AI 群聊；群聊及会议支持邀请新成员、重命名、长期保留和手动解散或删除。
- 新增常见 AI 品牌头像预设，以及支持拖动、滚轮缩放和正方形裁剪的头像上传工具。
- 每个群聊和会议自动加入可配置管理员，人类可以 `@管理员` 调整话题和协作状态。

### 协作控制台

- 新增任务板，支持负责人、开始任务、评审、完成、暂停和受阻等状态。
- 新增决策板，用候选方案、理由、风险和信心比较可行方案，由人类作最终选择，不再进行人气投票。
- 新增成果库和验收流程，支持登记文件、链接或结论，并进行验收、驳回或补充证据。
- 新增逐角色实时监控与共享协作板，可查看工具、子 Agent、最近动作和文件锁，减少多人编辑冲突。

### Token 与渠道限流保护

- 自动接话恢复为固定旧版流程：角色先独立判断，再由管理员过滤并选择下一位发言者；移除额外的判断方式选项。
- 保留自动接话总开关；每个 AI 用户可关闭自己的独立判断，改由管理员直接判断，以节省 Token。
- 新增可选的渠道限流冷却和同渠道请求队列。Arena 不读取或保存 API Key，因此按供应商配置识别共享渠道。
- 正式发言、接话判断、工具续跑、子 Agent、重试和失败请求全部纳入渠道保护；队列按每分钟 55 次的安全额度放行。
- 429 限流会使同一供应商配置下的角色统一冷却，并优先采用提供方返回的重试时间。

### 数据与兼容性

- 新增独立历史管理，可分类查看、重命名和删除会议、私聊与群聊。
- Arena 会话可自动归入专属 DSH 会话分组，避免干扰标准模式的会话列表。
- 运行数据仍只保存在本机 `DSH_HOME`；GitHub 发布包不包含 API Key、供应商配置、角色卡、上传头像或聊天记录。

## 0.2.0

- 新增可选的渠道限流冷却：共享供应商/API Key 触发限流后，相关角色统一冷却，并展示预计恢复时间。
- 失败请求纳入限流保护，避免同一渠道持续重试。
- 新增可选的同渠道请求队列，降低共享渠道触发 60 RPM 的概率；启用后可能降低回复速度。
- 新增自动接话判断总开关，关闭后不影响人类发言和 `@AI`。
- 新增 AI 用户级别的“关闭自动接话”选项。
- 新增“单独判断 / 管理员决定”两种自动接话策略，并补充请求与 Token 消耗提示。
- 持久化设置迁移至状态文件版本 4。
